touch stik.json

echo '{
  "imagenes": [
    "enlace_a_imagen_1",
    "enlace_a_imagen_2",
    "enlace_a_imagen_3",
    ...
  ]
}' > stik.json

git add stik.json

git commit -m "Agregando enlaces de imágenes"

git push origin main